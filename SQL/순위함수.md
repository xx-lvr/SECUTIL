## 순위함수
순위를 계산하는 함수이다. ```RANK, DENSE_RANK, ROW_NUMBER```가 있다.

+ RANK - 동일 순위는 같은 순위값을 가진다. 순위값은 앞 순위까지의 누적개수 +1이 된다.
```
-> 1,2,2,4,4,4,7
```
+ DENSE_RANK - 동일 순위는 같은 순위값을 가진다. 순위값은 단순하게 앞 순위 +1이다.
```
-> 1,2,2,3,3,3,4
```
+ ROW_NUMBER - 동일 순위라도 각각의 행이 고유의 순위값을 가진다.
```
-> 1,2,3,4,5,6,7
```